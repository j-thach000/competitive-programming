#include "bits/stdc++.h"

using namespace std;

int main() {
    // magic lines to make i/o faster, refer to https://stackoverflow.com/a/31165481 if you're curious why
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t; cin >> t;

    for (int i = 0; i < t; i++) {
        int n; int p; cin >> n >> p;
        int cost = p;
        
        // is there some sort of formula i'm missing?
        // input is 10^9 and i can't find a formula
        // unless i miscalculated the input

    }
}