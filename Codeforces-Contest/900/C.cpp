#include "bits/stdc++.h"

using namespace std;

int main() {
    // magic lines to make i/o faster, refer to https://stackoverflow.com/a/31165481 if you're curious why
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // can you choose k distinct integers from 1 to n that sum to x
    // n = 5, k = 3, x = 10
    // 1 2 3 4 5
    // 5 + 4 + 1

    // test case observations
    // 1) n > k, n < x; k < n x; x > n, k;
    // 2) n > 
    
    // long long needed
    // implement?
    // if the sum of 1 to n is >= x 
    // implies we know what satisfies n and x
    // what does k need to be 
    // 
    // 

    // 1 2 3 4  5
    // 1 3 6 10 15

    int t; cin >> t;
    for (int i = 0; i < t; i++) {
        int n, k, x; 
        cin >> n >> k >> x;

    }
}